# 開発方針

- React + TypeScript + Viteを使用する。
- IndexedDBの操作には`idb`ライブラリを使用する。
- 既存機能を壊さないことを最優先とする。
- IndexedDBに保存されている既存データとの互換性を維持する。
- 不要なライブラリを追加しない。
- 大規模なリファクタリングを勝手に行わない。
- TypeScriptの型をインポートするときは`import type`を使用する。
- Androidのダークモードでも、現在のライトテーマ固定を維持する。
- GPS情報は案件単位で保存する。
- 境界点は案件に紐付ける。
- 写真は境界点に紐付け、BlobとしてIndexedDBへ保存する。
- 修正後は必ず`npm run build`を実行する。
- ビルドエラーがある状態で作業完了としない。
