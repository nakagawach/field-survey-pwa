# 開発方針

- React + TypeScript + Viteを使用する。
- IndexedDBの操作には`idb`ライブラリを使用する。
- 既存機能を壊さないことを最優先とする。
- IndexedDBに保存されている既存データとの互換性を維持する。
- 不要なライブラリを追加しない。
- 大規模なリファクタリングを勝手に行わない。
- TypeScriptの型をインポートするときは`import type`を使用する。
- Androidのダークモードでも、現在のライトテーマ固定を維持する。
- GPS情報は案件単位で保存する。
- 境界点は案件に紐付ける。
- 写真は境界点に紐付け、BlobとしてIndexedDBへ保存する。
- 修正後は必ず`npm run build`を実行する。
- ビルドエラーがある状態で作業完了としない。

# Codex作業ルール

## 作業開始時

新しいタスクでは、過去タスクの状態を推測せず、最初に現在のリポジトリ状態を確認する。

可能な範囲で以下を確認する。

- 現在のブランチ
- HEADコミット
- git status
- 未コミット変更
- remote
- 直前のdiff

既存の未コミット変更がある場合、勝手に破棄・reset・上書きしない。

## 変更方針

- ユーザーから指定された範囲だけ変更する。
- 関係ないファイルを変更しない。
- 不要なリファクタリングを行わない。
- 不要なコード整形を行わない。
- 原因調査では複数の対策を同時に実施しない。
- 原因調査では仮説を1つずつ検証する。
- 根拠のないtimer、delay、setTimeout等を追加しない。
- 依頼と関係のない改善を同じPRへ混ぜない。
- 既存のユーザー変更を勝手に破棄しない。

## コード変更後

利用可能な場合、最低限以下を確認する。

- npm run build
- npm run lint
- git diff --check
- git diff --name-only
- git diff

テストscriptが存在する場合は、変更内容に応じて実行する。

実行できなかった確認項目は、成功したことにせず「未実施」とする。

## Git / Pull Request

PR作成を依頼された場合、環境上可能なら説明だけで終了せず、実際に以下まで実行する。

1. 差分確認
2. build / lint / check
3. commit
4. push
5. Pull Request作成
6. PR URL取得

ユーザーから明示的に依頼されていない限り、PRを勝手にmergeしない。

以下をユーザーの許可なく実行しない。

- git reset --hard
- git clean
- force push
- base branchへの直接push
- 既存変更の破棄

GitHub操作が環境上できない場合は、推測せず実際のエラーと未完了作業を報告する。


# タスク終了時の引き継ぎ【必須】

すべての開発タスク終了時に、ユーザーから個別に依頼されなくても、最後に必ず

「次タスク用引き継ぎ」

をチャット上へ出力する。

Codexの次のタスクは前タスクの会話内容を完全に把握していない前提で、単独で読んでも作業を継続できる内容にする。

最低限、以下を記載する。

## 基本情報

- リポジトリ
- 現在のブランチ
- base branch
- HEAD / commit hash

## 今回の目的

- 何を実現・調査するタスクだったか
- 原因調査の場合は、何の仮説を検証したか

## 実施内容

- 実際に行った変更
- 変更ファイル
- diff概要
- 変更理由

## 変更していない重要部分

依頼上、意図的に変更しなかった重要機能やファイルがある場合は記載する。

## Checks

以下について実際の結果を記載する。

- npm run build
- npm run lint
- git diff --check
- test
- その他実施した確認

それぞれ、

- PASS
- FAIL
- 未実施
- 該当なし

を明確にする。

## Git状態

- git status
- commit済み / 未コミット
- commit hash
- push済み / 未push
- PR作成済み / 未作成
- PR番号
- PR URL
- merge済み / 未merge

## 未完了事項

以下のような残作業を記載する。

- 実機確認
- Vercel Preview確認
- PR作成
- 次のA/Bテスト
- その他の残作業

## 次のタスクで最初に行うこと

次のCodexタスクが何から開始すべきか、具体的に記載する。

単に「続きから作業する」と書かず、最初の確認・コマンド・対象ファイル等が分かるようにする。

## 注意事項・禁止事項

次のタスクで、

- 壊してはいけない機能
- 変更してはいけないファイル
- 同時に試してはいけない対策
- 現在維持すべきA/Bテスト条件

などがある場合は具体的に記載する。


# 引き継ぎの正確性

引き継ぎでは「実施済み」と「予定」を混同しない。

確認できない情報は推測せず、

- 未確認
- 未実施
- 不明

と記載する。

存在しないcommit、branch、PR、URL、テスト結果を作らない。

実機でしか確認できない問題は、Codex上のbuild成功だけで「修正済み」と判断しない。


# 最終原則

迷った場合は、新しい変更を追加する前に現在の状態を確認する。

既存機能の維持を優先し、目的達成に必要な最小限の変更だけを行う。