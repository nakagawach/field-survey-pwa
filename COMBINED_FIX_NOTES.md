Combined mobile fix branch: preserves the tested camera input activation CSS change and removes the global ImmediateTouchButtons wrapper that programmatically clicked every touch button on pointerup and suppressed native clicks for 700ms. The project save validation in App.tsx is unchanged, so empty title still alerts only when Save itself is tapped. This file is temporary branch documentation and should be removed before merge.
